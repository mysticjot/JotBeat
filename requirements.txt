# JotBeat studio orchestrator dependencies (Phase 2)
langgraph>=1.2
langgraph-checkpoint-sqlite>=3.1
# models.py._call: one httpx client for all OpenAI-compatible providers
httpx>=0.27
# Gemini client (the one non-OpenAI-compatible provider)
google-genai>=1.0
# Asset/map generators (game/tools/build_kenney_assets.py, game/maps imagery)
Pillow>=10.0
